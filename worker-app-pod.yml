apiVersion: v1
kind: Pod
metadata:
  name: worker-app-pod
  labels:
    name: voting-app-pod
    app: demo-voting-app
spec:
  containers:
    - name: worker-app
      image: ddockersamples/examplevotingapp_worker